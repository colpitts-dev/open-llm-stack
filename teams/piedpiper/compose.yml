# GENERATED by scripts/team-render.py from teams/piedpiper/team.toml. Do not edit: edit team.toml, then `make team-render`.
# One service per member, extending teams/_base.yml (the shared shape). Included by docker-compose.yml through TEAM_NAME.
services:
  jared:
    extends: { file: teams/_base.yml, service: agent-base }
    profiles: [team]
    image: ghcr.io/block/buzz-sprig:sha-e17cdd9
    volumes:
      - team-jared:/home/agent
    environment:
      TEAM_MEMBER: jared
      TEAM_ROLE: coordinator
      TEAM_RUNTIME: buzz-agent
      BUZZ_ACP_DISPLAY_NAME: Jared
      BUZZ_PRIVATE_KEY: ${TEAM_JARED_PRIVATE_KEY:?run make team-render}
      BUZZ_ACP_SESSION_POLICY: channel
      BUZZ_ACP_RESPOND_TO_ALLOWLIST: ${TEAM_ALLOWLIST:-},${TEAM_SMOKE_PUBKEY:-},${CONSOLE_PUBKEY:-},${TEAM_JARED_PUBKEY:-},${TEAM_GILFOYLE_PUBKEY:-},${TEAM_DINESH_PUBKEY:-},${TEAM_MONICA_PUBKEY:-},${TEAM_ERLICH_PUBKEY:-}
      TEAM_ROSTER: "jared=coordinator=Jared=coordinator: routes every job and relays what is still open=${TEAM_JARED_PUBKEY:-};gilfoyle=builder=Gilfoyle=senior engineer: plans first, builds after approval=${TEAM_GILFOYLE_PUBKEY:-};dinesh=reviewer=Dinesh=developer and QA: reviews and checks conformance against the approved plan=${TEAM_DINESH_PUBKEY:-};monica=adversary=Monica=red team: attacks every PR blind, in her own channel=${TEAM_MONICA_PUBKEY:-};erlich=gatekeeper=Erlich=gatekeeper and judge: binary ship verdict from the evidence, plus the score, in his own channel=${TEAM_ERLICH_PUBKEY:-}"
      OPENAI_COMPAT_MODEL: qwen3.8-q5
      OPENAI_COMPAT_API_KEY: ${TEAM_JARED_LITELLM_KEY:-${LITELLM_MASTER_KEY}}
      GITEA_USER: jared
      GITEA_TOKEN: ${TEAM_JARED_GITEA_TOKEN:-}
      BUZZ_ACP_HEARTBEAT_INTERVAL: "0"
      BUZZ_ACP_HEARTBEAT_PROMPT_FILE: /opt/team/agents/roles/coordinator-heartbeat.md
  gilfoyle:
    extends: { file: teams/_base.yml, service: agent-base }
    profiles: [team]
    image: ghcr.io/block/buzz-sprig:sha-e17cdd9
    volumes:
      - team-gilfoyle:/home/agent
    environment:
      TEAM_MEMBER: gilfoyle
      TEAM_ROLE: builder
      TEAM_RUNTIME: buzz-agent
      BUZZ_ACP_DISPLAY_NAME: Gilfoyle
      BUZZ_PRIVATE_KEY: ${TEAM_GILFOYLE_PRIVATE_KEY:?run make team-render}
      BUZZ_ACP_SESSION_POLICY: thread
      BUZZ_ACP_RESPOND_TO_ALLOWLIST: ${TEAM_ALLOWLIST:-},${TEAM_SMOKE_PUBKEY:-},${CONSOLE_PUBKEY:-},${TEAM_JARED_PUBKEY:-},${TEAM_GILFOYLE_PUBKEY:-},${TEAM_DINESH_PUBKEY:-},${TEAM_MONICA_PUBKEY:-},${TEAM_ERLICH_PUBKEY:-}
      TEAM_ROSTER: "jared=coordinator=Jared=coordinator: routes every job and relays what is still open=${TEAM_JARED_PUBKEY:-};gilfoyle=builder=Gilfoyle=senior engineer: plans first, builds after approval=${TEAM_GILFOYLE_PUBKEY:-};dinesh=reviewer=Dinesh=developer and QA: reviews and checks conformance against the approved plan=${TEAM_DINESH_PUBKEY:-};monica=adversary=Monica=red team: attacks every PR blind, in her own channel=${TEAM_MONICA_PUBKEY:-};erlich=gatekeeper=Erlich=gatekeeper and judge: binary ship verdict from the evidence, plus the score, in his own channel=${TEAM_ERLICH_PUBKEY:-}"
      OPENAI_COMPAT_MODEL: qwen3.8-q5
      OPENAI_COMPAT_API_KEY: ${TEAM_GILFOYLE_LITELLM_KEY:-${LITELLM_MASTER_KEY}}
      GITEA_USER: gilfoyle
      GITEA_TOKEN: ${TEAM_GILFOYLE_GITEA_TOKEN:-}
  dinesh:
    extends: { file: teams/_base.yml, service: agent-base }
    profiles: [team]
    image: ghcr.io/block/buzz-sprig:sha-e17cdd9
    volumes:
      - team-dinesh:/home/agent
    environment:
      TEAM_MEMBER: dinesh
      TEAM_ROLE: reviewer
      TEAM_RUNTIME: buzz-agent
      BUZZ_ACP_DISPLAY_NAME: Dinesh
      BUZZ_PRIVATE_KEY: ${TEAM_DINESH_PRIVATE_KEY:?run make team-render}
      BUZZ_ACP_SESSION_POLICY: thread
      BUZZ_ACP_RESPOND_TO_ALLOWLIST: ${TEAM_ALLOWLIST:-},${TEAM_SMOKE_PUBKEY:-},${CONSOLE_PUBKEY:-},${TEAM_JARED_PUBKEY:-},${TEAM_GILFOYLE_PUBKEY:-},${TEAM_DINESH_PUBKEY:-},${TEAM_MONICA_PUBKEY:-},${TEAM_ERLICH_PUBKEY:-}
      TEAM_ROSTER: "jared=coordinator=Jared=coordinator: routes every job and relays what is still open=${TEAM_JARED_PUBKEY:-};gilfoyle=builder=Gilfoyle=senior engineer: plans first, builds after approval=${TEAM_GILFOYLE_PUBKEY:-};dinesh=reviewer=Dinesh=developer and QA: reviews and checks conformance against the approved plan=${TEAM_DINESH_PUBKEY:-};monica=adversary=Monica=red team: attacks every PR blind, in her own channel=${TEAM_MONICA_PUBKEY:-};erlich=gatekeeper=Erlich=gatekeeper and judge: binary ship verdict from the evidence, plus the score, in his own channel=${TEAM_ERLICH_PUBKEY:-}"
      OPENAI_COMPAT_MODEL: qwen3.8-q5
      OPENAI_COMPAT_API_KEY: ${TEAM_DINESH_LITELLM_KEY:-${LITELLM_MASTER_KEY}}
      GITEA_USER: dinesh
      GITEA_TOKEN: ${TEAM_DINESH_GITEA_TOKEN:-}
  monica:
    extends: { file: teams/_base.yml, service: agent-base }
    profiles: [team]
    image: ghcr.io/block/buzz-sprig:sha-e17cdd9
    volumes:
      - team-monica:/home/agent
    environment:
      TEAM_MEMBER: monica
      TEAM_ROLE: adversary
      TEAM_RUNTIME: buzz-agent
      BUZZ_ACP_DISPLAY_NAME: Monica
      BUZZ_PRIVATE_KEY: ${TEAM_MONICA_PRIVATE_KEY:?run make team-render}
      BUZZ_ACP_SESSION_POLICY: thread
      BUZZ_ACP_RESPOND_TO_ALLOWLIST: ${TEAM_ALLOWLIST:-},${TEAM_SMOKE_PUBKEY:-},${CONSOLE_PUBKEY:-},${TEAM_JARED_PUBKEY:-},${TEAM_GILFOYLE_PUBKEY:-},${TEAM_DINESH_PUBKEY:-},${TEAM_MONICA_PUBKEY:-},${TEAM_ERLICH_PUBKEY:-}
      TEAM_ROSTER: "jared=coordinator=Jared=coordinator: routes every job and relays what is still open=${TEAM_JARED_PUBKEY:-};gilfoyle=builder=Gilfoyle=senior engineer: plans first, builds after approval=${TEAM_GILFOYLE_PUBKEY:-};dinesh=reviewer=Dinesh=developer and QA: reviews and checks conformance against the approved plan=${TEAM_DINESH_PUBKEY:-};monica=adversary=Monica=red team: attacks every PR blind, in her own channel=${TEAM_MONICA_PUBKEY:-};erlich=gatekeeper=Erlich=gatekeeper and judge: binary ship verdict from the evidence, plus the score, in his own channel=${TEAM_ERLICH_PUBKEY:-}"
      OPENAI_COMPAT_MODEL: qwen3.8-q5
      OPENAI_COMPAT_API_KEY: ${TEAM_MONICA_LITELLM_KEY:-${LITELLM_MASTER_KEY}}
      GITEA_USER: monica
      GITEA_TOKEN: ${TEAM_MONICA_GITEA_TOKEN:-}
  erlich:
    extends: { file: teams/_base.yml, service: agent-base }
    profiles: [team]
    image: ghcr.io/block/buzz-sprig:sha-e17cdd9
    volumes:
      - team-erlich:/home/agent
    environment:
      TEAM_MEMBER: erlich
      TEAM_ROLE: gatekeeper
      TEAM_RUNTIME: buzz-agent
      BUZZ_ACP_DISPLAY_NAME: Erlich
      BUZZ_PRIVATE_KEY: ${TEAM_ERLICH_PRIVATE_KEY:?run make team-render}
      BUZZ_ACP_SESSION_POLICY: channel
      BUZZ_ACP_RESPOND_TO_ALLOWLIST: ${TEAM_ALLOWLIST:-},${TEAM_SMOKE_PUBKEY:-},${CONSOLE_PUBKEY:-},${TEAM_JARED_PUBKEY:-},${TEAM_GILFOYLE_PUBKEY:-},${TEAM_DINESH_PUBKEY:-},${TEAM_MONICA_PUBKEY:-},${TEAM_ERLICH_PUBKEY:-}
      TEAM_ROSTER: "jared=coordinator=Jared=coordinator: routes every job and relays what is still open=${TEAM_JARED_PUBKEY:-};gilfoyle=builder=Gilfoyle=senior engineer: plans first, builds after approval=${TEAM_GILFOYLE_PUBKEY:-};dinesh=reviewer=Dinesh=developer and QA: reviews and checks conformance against the approved plan=${TEAM_DINESH_PUBKEY:-};monica=adversary=Monica=red team: attacks every PR blind, in her own channel=${TEAM_MONICA_PUBKEY:-};erlich=gatekeeper=Erlich=gatekeeper and judge: binary ship verdict from the evidence, plus the score, in his own channel=${TEAM_ERLICH_PUBKEY:-}"
      OPENAI_COMPAT_MODEL: qwen3.8-q5
      OPENAI_COMPAT_API_KEY: ${TEAM_ERLICH_LITELLM_KEY:-${LITELLM_MASTER_KEY}}
      GITEA_USER: erlich
      GITEA_TOKEN: ${TEAM_ERLICH_GITEA_TOKEN:-}
volumes:
  team-jared:
  team-gilfoyle:
  team-dinesh:
  team-monica:
  team-erlich:
